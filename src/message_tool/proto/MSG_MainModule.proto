syntax = "proto3";
package MSG_MainModule;

// add by stefanchen 
// 主模块消息结构
enum MAINMSG
{
    Begin = 0; //起点消息
    SERVER = 1; //服务器间消息 for each server
    LOGIN = 2;  //登陆模块消息 for login server
    RPC = 3;    // rpc call 
    // for game server
    PLAYER = 4; //玩家消息
}

